var webpack = require('webpack');
var path = require('path');

module.exports = {
  entry: ['./src/facebook-login.js'],
  module: {
    loaders: [
      {
        test: /\.js$/,
        exclude: /node_modules/,
        loader: 'babel-loader',
        query: {
          presets: ['es2015'],
          plugins: ['transform-runtime']
        }
      }
    ],
    preLoaders: [
      {
        test: /\.js$/,
        exclude: /node_modules/,
        loader: 'eslint-loader'
      }
    ],
  },
  output: {
    path: __dirname + '/lib',
    filename: 'facebook-login.js',
    libraryTarget: 'umd'
  }
};
